DJANGO_SECRET_KEY=change-me
DEBUG=1
ALLOWED_HOSTS=*
# Use SQLite by default (no config needed). For Postgres, set these:
DB_ENGINE=sqlite
DB_NAME=zenova.sqlite3
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=127.0.0.1
DB_PORT=5432

# Frontend origins you’ll use later
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
